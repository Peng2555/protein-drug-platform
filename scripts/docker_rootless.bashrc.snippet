# Docker Rootless — add to ~/.bashrc (optional, for manual docker commands)
# echo 'source /home/pengpai/data/Company_Project/Boltz2/scripts/docker_rootless.env' >> ~/.bashrc

export DOCKER_HOST=unix:///run/user/1016/docker.sock
export PATH=/usr/bin:$PATH
